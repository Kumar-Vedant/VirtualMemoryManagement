#include <bits/stdc++.h>

class Task
{
    public:
        Task(int taskID, int logicalAddress, int size)
        {
            // request the memory manager for the required space
            
        }
};